services:
  landing:
    image: nginx:alpine
    container_name: handbook-landing
    restart: unless-stopped
    ports:
      - "${LANDING_PORT}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ../doc/.vitepress/dist:/usr/share/nginx/html:ro
